# Deployment Notes

- `R2_PUBLIC_BASE_URL` currently uses the Cloudflare `r2.dev` public URL for early testing.
- Replace it with the production media domain before the final public launch.
- The newspaper importer is intentionally marked coming soon in the UI; no Groq key is required for the current launch.
